# Alice's Protocol - Airalogy 模板类型学习指南

**目标：** 利用 CCK-8 法评估某药物对肿瘤细胞增殖的抑制作用。

> [!NOTE] 📚 关于本文档
> 本文档是一个完整的 Airalogy Markdown (AIMD) 学习示例，展示了所有可用的模板类型。
> 每个模板类型都配有 callout 说明，方便你边学边用。

---

## 基本信息

> [!TIP] 🏷️ `{{var|<var_id>}}` - 变量模板
> **用途：** 最通用的数据收集字段，用于收集用户输入的数据。
> **语法：** `{{var|<var_id>}}`
> **规则：**
> - `var_id` 不能以下划线开头
> - 遵循 Python 标识符规则
> - 在整个协议中必须唯一
> - 默认为字符串类型，可通过 Pydantic 模型定义其他类型

> [!TIP] 🔢 `{{var|<var_id>: <type> = <default>}}` - 带类型的变量模板
> **用途：** 直接在 AIMD 文件中定义变量的类型、默认值和约束。
> **语法：** `{{var|<var_id>: <type> = <default_value>, **kwargs}}`
> **支持的类型：**
> - `str` - 字符串（可添加 `max_length`, `min_length` 约束）
> - `int` - 整数（可添加 `gt`, `ge`, `lt`, `le`, `multiple_of` 约束）
> - `float` - 浮点数（同整数约束）
> - `bool` - 布尔值
> - `date` - 日期（YYYY-MM-DD 格式）
> - `datetime` - 日期时间（ISO 8601 格式）
> - `list[T]` - 列表类型
> **可选参数：**
> - `title` - 字段标题
> - `description` - 字段描述
> - `ge/gt/le/lt` - 数值约束（大于等于/大于/小于等于/小于）
> - `max_length/min_length` - 字符串长度约束

实验者姓名：{{var|experimenter_name: str = "张三", title="实验者", description="负责本次实验的研究人员"}}

实验时间：{{var|experiment_time: datetime, title="实验时间", description="实验开始的日期和时间"}}

实验编号：{{var|experiment_number: int = 1, title="实验编号", ge=1, description="本次实验的序号"}}

---

## 实验步骤

> [!TIP] � `{{step|<srtep_id>, <level>, check=True}}` - 步骤模板
> **用途：** 用于组织工作流程，支持 1-3 级层级。
> **语法：** `{{step|<step_id>[, <level>][, check=True][, checked_message="..."]}}`
> **参数说明：**
> - `step_id`: 步骤的唯一标识符
> - `level`: 层级（1-3），默认为 1
> - `check=True`: 可选，启用复选框
> - `checked_message`: 可选，勾选后显示的消息

## 细胞铺板

{{step|cell_seeding}} 细胞铺板

将 {{var|cell_line_name: str, title="细胞系名称", description="使用的肿瘤细胞系"}} 细胞以每孔 {{var|seeding_density: int = 5000, title="接种密度", ge=1000, le=50000, description="每孔接种的细胞数量"}} 个细胞的密度播种到 96 孔板中。  
培养 {{var|adhesion_time: float = 24.0, title="贴壁时间", ge=0, description="细胞贴壁所需的培养时间（小时）"}} 小时，以便细胞贴壁。

> [!NOTE] 二级步骤示例
> 使用数字 `2` 表示这是一个二级子步骤

{{step|prepare_plate, 2, check=True}} 准备 96 孔板
- 检查孔板是否洁净
- 标记对照组和处理组位置

{{step|seed_cells, 2}} 移液操作
- 吸取细胞悬液
- 小心加入孔中

---

## 进阶：Step 内容分组与打断机制

> [!TIP] 🧩 **Step 内容归属规则**
> 下面的示例展示了 Step 卡片的范围是如何确定的，以及如何使用 **标题** 和 **分割线** 来有意“打断” Step，从而创建独立的文本说明区块。

{{step|demo_standard}} 1. 标准 Step 范围
这是 Step 内部的内容。
它会一直延伸，直到遇到下一个 Step 标记。

{{step|demo_header}} 2. 被标题打断的 Step
这是 Step 内部的内容。
注意看下方，Step 卡片会在标题处截断。

### ⚡️ 这是一个标题 (Header)
**注意：** 因为上方出现了标题，所以 Step 卡片已经结束。
这个区域不再属于 `demo_header` Step，而是一个独立的文本展示区。
*技巧：你可以在这里写一些长篇的原理介绍，通过这种方式避免 Step 卡片内容看起来过长。*

{{step|demo_divider}} 3. 被分割线打断的 Step
这是 Step 内部的内容。

---

**注意：** 由于上方使用了分割线 (`---`)，Step 卡片在此结束。
现在的这段文字也是独立的。

---

## 关键检查点

> [!TIP] ✅ `{{check|<check_id>}}` - 检查点模板
> **用途：** 关键验证点，总是显示复选框，用于确保重要步骤已完成。
> **语法：** `{{check|<check_id>[, checked_message="..."]}}`
> **特点：**
> - 与 `step` 的 `check=True` 不同，`check` 模板始终显示复选框
> - 适用于必须确认的关键节点
> - 可添加 `checked_message` 在勾选后显示提示

{{check|check_cell_attachment}} 在继续操作前，请确保细胞已经充分贴壁且状态良好。

{{check|safety_compliance, checked_message="已确认安全合规"}} 确认已穿戴好实验服、手套和护目镜。

---

## 药物浓度梯度配制

> [!TIP] 📊 `{{var|<var_id>, subvars=[...]}}` - 变量表格模板
> **用途：** 创建可编辑的表格，每行包含多个子变量，适合收集结构化数据。
> **基础语法：** `{{var|<var_id>, subvars=[col1, col2, ...]}}`
> **带列名语法：** 
> ```aimd
> {{var|table_id,
>     title="表格标题",
>     subvars=[
>         var(col1: float, title="列名1 (单位)"),
>         var(col2: str, title="列名2")
>     ]
> }}
> ```
> **特点：**
> - 本质上仍是变量，数据存储在 var 命名空间下
> - 使用 `var(...)` 语法可为子变量定义 `title`（列名）和 `description`
> - 列名可包含单位信息，如 `"浓度 (μM)"`、`"体积 (mL)"`
> **适用场景：**
> - 记录多个样本的测量数据
> - 配制不同浓度的药物溶液
> - 记录多孔板各孔的 OD 值

{{step|drug_preparation}} 药物浓度梯度配制

使用 {{var|drug_name: str, title="药物名称", description="待测试的药物名称"}} 配制浓度梯度溶液。
母液浓度：{{var|stock_concentration: float = 1000.0, title="母液浓度", ge=0, description="药物母液浓度（μM）"}} μM

{{step|prepare_dilutions, 2, check=True}} 配制稀释液

请在下表中记录每个浓度梯度的配制信息：

{{var|drug_dilutions,
    title="药物浓度梯度配制表",
    description="记录各浓度梯度的配制详情",
    subvars=[
        var(concentration: float = 0.0, title="最终浓度 (μM)", description="稀释后的药物浓度"),
        var(stock_volume: float = 0.0, title="母液用量 (μL)", description="从母液中取用的体积"),
        var(solvent_volume: float = 100.0, title="溶剂用量 (μL)", description="添加的溶剂体积"),
        var(well_position: str = "", title="孔位", description="96孔板中的位置，如 A1, B2")
    ]
}}

> [!EXAMPLE] 变量表格列名定义
> 使用 `var(...)` 语法可以为每个子变量定义 `title`（列名）、`description`（描述）和默认值：
> ```aimd
> {{var|table_name, subvars=[
>     var(col1: float = 0.0, title="列名1 (单位)", description="说明"),
>     var(col2: str = "默认值", title="列名2", description="说明")
> ]}}
> ```
> 
> **默认值说明：**
> - 子变量默认值：新增行时，每列会使用定义的默认值
> - 表格整体默认值：在 `model.py` 中使用 `default_factory` 预填充行数据

{{step|add_to_wells, 2, check=True, checked_message="Drug added"}} 加入孔板
- 按照上表将各浓度药物加入对应孔位
- 对照组加入等体积溶剂

培养 {{var|treatment_duration: float = 48.0, title="处理时间", ge=1, le=168, description="药物处理的持续时间（小时）"}} 小时。

---

## 引用模板演示

> [!TIP] 🔗 `{{ref_step|<step_id>}}` - 步骤引用模板
> **用途：** 在文档中多次引用同一个步骤，保持一致性。
> **语法：** `{{ref_step|<step_id>}}`
> **特点：**
> - 不会创建新字段，只是显示已存在步骤的引用
> - 适合在后续说明中回顾之前的步骤
> - 点击引用可跳转到原步骤位置

根据 {{ref_step|cell_seeding}} 中的操作，细胞应该已经完成铺板。

如果在 {{ref_step|drug_preparation}} 过程中出现问题，请重新检查药物配制。

> [!TIP] 🔗 `{{ref_var|<var_id>}}` - 变量引用模板
> **用途：** 在文档中多次引用同一个变量的值。
> **语法：** `{{ref_var|<var_id>}}`
> **特点：**
> - 不会创建新的输入字段
> - 显示变量的当前值
> - 适合在总结或计算说明中引用之前输入的数据

本实验使用的细胞系为 {{ref_var|cell_line_name}}，药物为 {{ref_var|drug_name}}，母液浓度为 {{ref_var|stock_concentration}} μM。

---

## 细胞活力检测

{{step|cell_viability_assay}} 细胞活力检测

加入 CCK-8 试剂，并进行相应的培养。  
使用酶标仪测定光密度（OD）。

{{step|add_reagent, 2}} 加入 CCK-8
- 每孔加入 10μL CCK-8 试剂
- 避免产生气泡
- 37°C 孵育 {{var|cck8_incubation_time: float = 2.0, title="CCK-8孵育时间", ge=0.5, le=4, description="CCK-8试剂孵育时间（小时）"}} 小时

{{step|measure_od, 2, check=True}} 酶标仪读数

设置波长 450nm，逐孔测量 OD 值，记录于下表：

{{var|od_measurements,
    title="OD值测量记录表",
    description="记录各孔的吸光度测量数据",
    subvars=[
        var(well_position: str = "", title="孔位", description="96孔板中的位置"),
        var(od_value: float = 0.0, title="OD值 (450nm)", description="吸光度值"),
        var(group_type: str = "处理组", title="分组", description="对照组/处理组/空白组"),
        var(concentration: float = 0.0, title="药物浓度 (μM)", description="该孔对应的药物浓度，对照组填0")
    ]
}}

> [!NOTE] 列名定义说明
> 表格的列名通过 `title` 参数定义，可以包含单位信息：
> - `title="OD值 (450nm)"` → 列名显示为 "OD值 (450nm)"
> - `title="药物浓度 (μM)"` → 列名显示为 "药物浓度 (μM)"

{{check|od_measurement_complete, checked_message="OD值测量完成"}} 确认所有孔位的 OD 值已记录完毕。

---

## 数据分析与抑制率计算

{{step|calculate_inhibition_rate}} 计算抑制率

根据测量的 OD 值计算各浓度下的细胞抑制率。

空白对照 OD 均值：{{var|blank_od_mean: float, title="空白OD均值", ge=0, description="空白孔OD值的平均值"}}
阴性对照 OD 均值：{{var|control_od_mean: float, title="对照组OD均值", ge=0, description="对照组OD值的平均值"}}

请在下表中记录各浓度的抑制率计算结果：

{{var|inhibition_results,
    title="抑制率计算结果表",
    description="各浓度下的细胞抑制率",
    subvars=[
        var(concentration: float = 0.0, title="药物浓度 (μM)", description="测试浓度"),
        var(treatment_od_mean: float = 0.0, title="处理组OD均值", description="该浓度处理组的OD平均值"),
        var(inhibition_rate: float = 0.0, title="抑制率 (%)", description="计算得到的抑制百分比"),
        var(notes: str = "", title="备注", description="补充说明")
    ]
}}

> [!INFO] 抑制率计算公式
> 抑制率 (%) = [1 - (处理组OD - 空白OD) / (对照组OD - 空白OD)] × 100%
> 
> 其中：
> - 空白 OD = {{ref_var|blank_od_mean}}
> - 对照组 OD = {{ref_var|control_od_mean}}

---

## 实验总结

{{check|experiment_complete, checked_message="实验已完成"}} 确认所有步骤已完成，数据已记录。

### 实验信息汇总

| 项目 | 值 |
|------|-----|
| 实验者 | {{ref_var|experimenter_name}} |
| 实验时间 | {{ref_var|experiment_time}} |
| 细胞系 | {{ref_var|cell_line_name}} |
| 药物名称 | {{ref_var|drug_name}} |
| 母液浓度 | {{ref_var|stock_concentration}} μM |
| 处理时间 | {{ref_var|treatment_duration}} 小时 |

---

## 📖 模板类型速查表

> [!ABSTRACT] 所有模板类型一览
> 
> | 模板类型 | 语法 | 用途 |
> |---------|------|------|
> | 变量 | `{{var\|<var_id>}}` | 收集用户输入数据 |
> | 带类型变量 | `{{var\|<var_id>: <type> = <default>}}` | 带类型约束的变量 |
> | 步骤 | `{{step\|<step_id>[, level][, check=True]}}` | 组织工作流程 |
> | 检查点 | `{{check\|<check_id>}}` | 关键验证点 |
> | 变量表格 | `{{var\|<var_id>, subvars=[...]}}` | 创建可编辑表格 |
> | 步骤引用 | `{{ref_step\|<step_id>}}` | 引用已定义的步骤 |
> | 变量引用 | `{{ref_var\|<var_id>}}` | 引用变量的值 |

> [!WARNING] 命名规则
> 所有字段 ID 都遵循相同的命名规则：
> - 不能以下划线开头
> - 遵循 Python 标识符规则
> - 在整个协议中必须唯一

> [!INFO] 变量类型定义方式
> **方式一：内联定义（推荐简单场景）**
> ```aimd
> {{var|age: int = 18, ge=0, title="年龄"}}
> {{var|name: str = "张三", max_length=50}}
> {{var|temperature: float = 37.0, ge=0, le=100}}
> ```
> 
> **方式二：Pydantic 模型（推荐复杂场景）**
> ```python
> from pydantic import BaseModel, Field
> 
> class VarModel(BaseModel):
>     age: int = Field(default=18, ge=0, title="年龄")
>     name: str = Field(default="张三", max_length=50)
> ```

> [!INFO] 高级功能
> - 所有字段类型都支持通过 Assigner 系统进行自动计算
> - 变量可以通过 Pydantic 模型定义类型、默认值和验证规则
> - 引用模板不会创建新字段，只是显示已存在字段的值或引用
